import { Route, Routes} from 'react-router-dom'

export const MainRoutes = () =>{
    return(
        <Routes>
            <Route path='/' element={} />
            <Route path='/accont/:name' element={} />
            <Route path='/account/info' element={} />
        </Routes>
    )
}